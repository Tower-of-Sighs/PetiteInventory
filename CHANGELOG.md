# 更新日志

## 1.1.0

### 模块化重构

- 完成 `common` 与 Forge 目标的多模块拆分。
- 分离 API、配置、物品栏服务、编辑服务、兼容层、平台事件和 Mixin。
- 统一物品占位、替换、堆叠、快速转移和掉落处理的规则。
- 将容器自动化处理与物品栏交互逻辑拆分，减少跨模块耦合。

### 编辑模式

- 新增 `/petiteinventory edit` 编辑模式开关。
- 编辑模式下支持八方向调整物品尺寸，只有鼠标位于可调整边缘或角落时才显示红色边框。
- 支持 Ctrl 多选、拖拽框选和 Shift 扩展选择。
- 右键打开预设颜色调色盘，支持批量修改颜色。
- 长按右键 0.5 秒后可以框选区域并批量设置占位尺寸。
- 每个容器 Screen 顶部提供 Petite 布局开关。
- 新增 `/petiteinventory blacklist` 和 `/petiteinventory whitelist`，用于切换 Screen 布局的默认启用逻辑。

### 物品栏与交互

- 支持使用鼠标滚轮或 `R` 键旋转长宽不等的物品实例。
- 物品替换支持大物品与小物品之间的双向替换。
- 右键放置会在原版点击处理前校验完整占位区域，防止越界、冲突或空间不足时强行放入物品。
- Shift 快速转移会按快捷栏、主物品栏的顺序寻找可用位置，并检查完整物品面积。
- 快捷栏与 Petite 主物品栏布局分离，多格物品不会跨越两组槽位。
- 修复偶数列容器的网格宽度计算和物品合并时的占位判断。
- 修复交易、铁砧、锻造台等结果槽快速转移造成的重复物品问题。
- 改进物品整体悬停、高亮和占位区域渲染，避免多格物品重复渲染。
- 改进漏斗和其他自动化路径的面积感知插入处理。

### 模组兼容

- 完善 Sophisticated Core 与 Sophisticated Backpacks 适配，支持实际面积渲染、占位校验和面积感知的快速转移。
- 分离精妙背包的存储区域、玩家主物品栏和快捷栏。
- 支持 TACZ `modern_kinetic_gun` 的 `GunId` NBT 尺寸和背景色匹配。
- 保留 KubeJS 事件桥接入口。

### 配置与发行

- 新增 `border_items.json`、`border_colors.json`、`screen_layouts.json` 和 `screen_layout_mode.json` 配置文件。
- 物品背景格改为动态读取当前物品栏背景材质，支持材质包适配。
- 移除固定的 `area.png` 资源。
- 移除旧的 `/size` 尺寸指令和颜色调整指令，改用编辑模式与配置文件。
- 项目许可证改为 GNU AGPL v3.0。
